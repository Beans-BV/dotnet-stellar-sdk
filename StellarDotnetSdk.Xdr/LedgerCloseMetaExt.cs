// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  union LedgerCloseMetaExt switch (int v)
//  {
//  case 0:
//      void;
//  case 1:
//      LedgerCloseMetaExtV1 v1;
//  };

//  ===========================================================================
public class LedgerCloseMetaExt
{
    public int Discriminant { get; set; }

    public LedgerCloseMetaExtV1 V1 { get; set; }

    public static void Encode(XdrDataOutputStream stream, LedgerCloseMetaExt encodedLedgerCloseMetaExt)
    {
        stream.WriteInt(encodedLedgerCloseMetaExt.Discriminant);
        switch (encodedLedgerCloseMetaExt.Discriminant)
        {
            case 0:
                break;
            case 1:
                LedgerCloseMetaExtV1.Encode(stream, encodedLedgerCloseMetaExt.V1);
                break;
            default:
                throw new InvalidDataException("Unknown discriminant value: " + encodedLedgerCloseMetaExt.Discriminant);
        }
    }

    public static LedgerCloseMetaExt Decode(XdrDataInputStream stream, int maxDepth)
    {
        if (maxDepth <= 0)
            throw new InvalidDataException("Maximum decoding depth reached while decoding LedgerCloseMetaExt");
        maxDepth -= 1;
        var decodedLedgerCloseMetaExt = new LedgerCloseMetaExt();
        var discriminant = stream.ReadInt();
        decodedLedgerCloseMetaExt.Discriminant = discriminant;
        switch (decodedLedgerCloseMetaExt.Discriminant)
        {
            case 0:
                break;
            case 1:
                decodedLedgerCloseMetaExt.V1 = LedgerCloseMetaExtV1.Decode(stream, maxDepth);
                break;
            default:
                throw new InvalidDataException("Unknown discriminant value: " + discriminant);
        }

        return decodedLedgerCloseMetaExt;
    }

    public static LedgerCloseMetaExt Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }
}
