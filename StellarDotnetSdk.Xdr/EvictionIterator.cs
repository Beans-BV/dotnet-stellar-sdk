// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  struct EvictionIterator {
//      uint32 bucketListLevel;
//      bool isCurrBucket;
//      uint64 bucketFileOffset;
//  };

//  ===========================================================================
public class EvictionIterator
{
    public Uint32 BucketListLevel { get; set; }
    public bool IsCurrBucket { get; set; }
    public Uint64 BucketFileOffset { get; set; }

    public static void Encode(XdrDataOutputStream stream, EvictionIterator encodedEvictionIterator)
    {
        Uint32.Encode(stream, encodedEvictionIterator.BucketListLevel);
        stream.WriteInt(encodedEvictionIterator.IsCurrBucket ? 1 : 0);
        Uint64.Encode(stream, encodedEvictionIterator.BucketFileOffset);
    }

    public static EvictionIterator Decode(XdrDataInputStream stream, int maxDepth)
    {
        if (maxDepth <= 0)
            throw new InvalidDataException("Maximum decoding depth reached");
        maxDepth -= 1;
        var decodedEvictionIterator = new EvictionIterator();
        decodedEvictionIterator.BucketListLevel = Uint32.Decode(stream, maxDepth);
        decodedEvictionIterator.IsCurrBucket = stream.ReadInt() == 1 ? true : false;
        decodedEvictionIterator.BucketFileOffset = Uint64.Decode(stream, maxDepth);
        return decodedEvictionIterator;
    }

    public static EvictionIterator Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }
}
