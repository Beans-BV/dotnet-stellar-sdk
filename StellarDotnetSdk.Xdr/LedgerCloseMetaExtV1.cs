// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  struct LedgerCloseMetaExtV1
//  {
//      ExtensionPoint ext;
//      int64 sorobanFeeWrite1KB;
//  };

//  ===========================================================================
public class LedgerCloseMetaExtV1
{
    public ExtensionPoint Ext { get; set; }
    public Int64 SorobanFeeWrite1KB { get; set; }

    public static void Encode(XdrDataOutputStream stream, LedgerCloseMetaExtV1 encodedLedgerCloseMetaExtV1)
    {
        ExtensionPoint.Encode(stream, encodedLedgerCloseMetaExtV1.Ext);
        Int64.Encode(stream, encodedLedgerCloseMetaExtV1.SorobanFeeWrite1KB);
    }

    public static LedgerCloseMetaExtV1 Decode(XdrDataInputStream stream, int maxDepth)
    {
        if (maxDepth <= 0)
            throw new IOException("Maximum decoding depth reached");
        maxDepth -= 1;
        var decodedLedgerCloseMetaExtV1 = new LedgerCloseMetaExtV1();
        decodedLedgerCloseMetaExtV1.Ext = ExtensionPoint.Decode(stream, maxDepth);
        decodedLedgerCloseMetaExtV1.SorobanFeeWrite1KB = Int64.Decode(stream, maxDepth);
        return decodedLedgerCloseMetaExtV1;
    }

    public static LedgerCloseMetaExtV1 Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }
}
