// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  typedef int64 SequenceNumber;

//  ===========================================================================
public class SequenceNumber
{
    public SequenceNumber()
    {
    }

    public SequenceNumber(Int64 value)
    {
        InnerValue = value;
    }

    public Int64 InnerValue { get; set; }

    public static void Encode(XdrDataOutputStream stream, SequenceNumber encodedSequenceNumber)
    {
        Int64.Encode(stream, encodedSequenceNumber.InnerValue);
    }

    public static SequenceNumber Decode(XdrDataInputStream stream, int maxDepth)
    {
        if (maxDepth <= 0)
            throw new InvalidDataException("Maximum decoding depth reached while decoding SequenceNumber");
        maxDepth -= 1;
        var decodedSequenceNumber = new SequenceNumber();
        decodedSequenceNumber.InnerValue = Int64.Decode(stream, maxDepth);
        return decodedSequenceNumber;
    }

    public static SequenceNumber Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }
}
