// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  union ContractExecutable switch (ContractExecutableType type)
//  {
//  case CONTRACT_EXECUTABLE_WASM:
//      Hash wasm_hash;
//  case CONTRACT_EXECUTABLE_STELLAR_ASSET:
//      void;
//  };

//  ===========================================================================
public class ContractExecutable
{
    public ContractExecutableType Discriminant { get; set; } = new();

    public Hash WasmHash { get; set; }

    public static void Encode(XdrDataOutputStream stream, ContractExecutable encodedContractExecutable)
    {
        stream.WriteInt((int)encodedContractExecutable.Discriminant.InnerValue);
        switch (encodedContractExecutable.Discriminant.InnerValue)
        {
            case ContractExecutableType.ContractExecutableTypeEnum.CONTRACT_EXECUTABLE_WASM:
                Hash.Encode(stream, encodedContractExecutable.WasmHash);
                break;
            case ContractExecutableType.ContractExecutableTypeEnum.CONTRACT_EXECUTABLE_STELLAR_ASSET:
                break;
        }
    }

    public static ContractExecutable Decode(XdrDataInputStream stream, int maxDepth)
    {
        if (maxDepth <= 0)
            throw new InvalidDataException("Maximum decoding depth reached");
        maxDepth -= 1;
        var decodedContractExecutable = new ContractExecutable();
        var discriminant = ContractExecutableType.Decode(stream, maxDepth);
        decodedContractExecutable.Discriminant = discriminant;
        switch (decodedContractExecutable.Discriminant.InnerValue)
        {
            case ContractExecutableType.ContractExecutableTypeEnum.CONTRACT_EXECUTABLE_WASM:
                decodedContractExecutable.WasmHash = Hash.Decode(stream, maxDepth);
                break;
            case ContractExecutableType.ContractExecutableTypeEnum.CONTRACT_EXECUTABLE_STELLAR_ASSET:
                break;
            default:
                throw new InvalidDataException("Unknown discriminant value: " + discriminant.InnerValue);
        }

        return decodedContractExecutable;
    }

    public static ContractExecutable Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }
}
