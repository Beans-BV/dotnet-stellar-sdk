// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  struct SCNonceKey {
//      int64 nonce;
//  };

//  ===========================================================================
public class SCNonceKey
{
    public Int64 Nonce { get; set; }

    public static void Encode(XdrDataOutputStream stream, SCNonceKey encodedSCNonceKey)
    {
        Int64.Encode(stream, encodedSCNonceKey.Nonce);
    }

    public static SCNonceKey Decode(XdrDataInputStream stream, int maxDepth)
    {
        if (maxDepth <= 0)
            throw new InvalidDataException("Maximum decoding depth reached while decoding SCNonceKey");
        maxDepth -= 1;
        var decodedSCNonceKey = new SCNonceKey();
        decodedSCNonceKey.Nonce = Int64.Decode(stream, maxDepth);
        return decodedSCNonceKey;
    }

    public static SCNonceKey Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }
}
