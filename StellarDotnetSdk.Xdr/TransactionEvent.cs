// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  struct TransactionEvent {    
//      TransactionEventStage stage;  // Stage at which an event has occurred.
//      ContractEvent event;  // The contract event that has occurred.
//  };

//  ===========================================================================
public class TransactionEvent
{
    public TransactionEventStage Stage { get; set; }
    public ContractEvent Event { get; set; }

    public static void Encode(XdrDataOutputStream stream, TransactionEvent encodedTransactionEvent)
    {
        TransactionEventStage.Encode(stream, encodedTransactionEvent.Stage);
        ContractEvent.Encode(stream, encodedTransactionEvent.Event);
    }

    public static TransactionEvent Decode(XdrDataInputStream stream)
    {
        var decodedTransactionEvent = new TransactionEvent();
        decodedTransactionEvent.Stage = TransactionEventStage.Decode(stream);
        decodedTransactionEvent.Event = ContractEvent.Decode(stream);
        return decodedTransactionEvent;
    }
}