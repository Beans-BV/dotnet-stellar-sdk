// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  struct FloodAdvert
//  {
//      TxAdvertVector txHashes;
//  };

//  ===========================================================================
public class FloodAdvert
{
    public TxAdvertVector TxHashes { get; set; }

    public static void Encode(XdrDataOutputStream stream, FloodAdvert encodedFloodAdvert)
    {
        TxAdvertVector.Encode(stream, encodedFloodAdvert.TxHashes);
    }

    public static FloodAdvert Decode(XdrDataInputStream stream, int maxDepth)
    {
        if (maxDepth <= 0)
            throw new InvalidDataException("Maximum decoding depth reached while decoding FloodAdvert");
        maxDepth -= 1;
        var decodedFloodAdvert = new FloodAdvert();
        decodedFloodAdvert.TxHashes = TxAdvertVector.Decode(stream, maxDepth);
        return decodedFloodAdvert;
    }

    public static FloodAdvert Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }
}
