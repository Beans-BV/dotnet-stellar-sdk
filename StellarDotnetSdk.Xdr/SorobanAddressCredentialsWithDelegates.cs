// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  struct SorobanAddressCredentialsWithDelegates
//  {
//      SorobanAddressCredentials addressCredentials;
//      SorobanDelegateSignature delegates<>;
//  };

//  ===========================================================================
public class SorobanAddressCredentialsWithDelegates
{
    public SorobanAddressCredentials AddressCredentials { get; set; }
    public SorobanDelegateSignature[] Delegates { get; set; }

    public static void Encode(XdrDataOutputStream stream, SorobanAddressCredentialsWithDelegates encodedSorobanAddressCredentialsWithDelegates)
    {
        SorobanAddressCredentials.Encode(stream, encodedSorobanAddressCredentialsWithDelegates.AddressCredentials);
        var delegatesSize = encodedSorobanAddressCredentialsWithDelegates.Delegates.Length;
        stream.WriteInt(delegatesSize);
        for (var i = 0; i < delegatesSize; i++)
        {
            SorobanDelegateSignature.Encode(stream, encodedSorobanAddressCredentialsWithDelegates.Delegates[i]);
        }
    }

    public static SorobanAddressCredentialsWithDelegates Decode(XdrDataInputStream stream, int maxDepth)
    {
        if (maxDepth <= 0)
            throw new InvalidDataException("Maximum decoding depth reached while decoding SorobanAddressCredentialsWithDelegates");
        maxDepth -= 1;
        var decodedSorobanAddressCredentialsWithDelegates = new SorobanAddressCredentialsWithDelegates();
        decodedSorobanAddressCredentialsWithDelegates.AddressCredentials = SorobanAddressCredentials.Decode(stream, maxDepth);
        var delegatesSize = stream.ReadInt();
        if (delegatesSize < 0)
            throw new InvalidDataException("delegates size " + delegatesSize + " is negative");
        var delegatesRemainingInputLen = stream.GetRemainingInputLen();
        if (delegatesRemainingInputLen >= 0 && delegatesRemainingInputLen < delegatesSize)
            throw new InvalidDataException("delegates size " + delegatesSize + " exceeds remaining input length " + delegatesRemainingInputLen);
        decodedSorobanAddressCredentialsWithDelegates.Delegates = new SorobanDelegateSignature[delegatesSize];
        for (var i = 0; i < delegatesSize; i++)
        {
            decodedSorobanAddressCredentialsWithDelegates.Delegates[i] = SorobanDelegateSignature.Decode(stream, maxDepth);
        }
        return decodedSorobanAddressCredentialsWithDelegates;
    }

    public static SorobanAddressCredentialsWithDelegates Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }
}
