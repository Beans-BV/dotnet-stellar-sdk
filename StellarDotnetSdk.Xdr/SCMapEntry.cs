// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  struct SCMapEntry
//  {
//      SCVal key;
//      SCVal val;
//  };

//  ===========================================================================
public class SCMapEntry
{
    public SCVal Key { get; set; }
    public SCVal Val { get; set; }

    public static void Encode(XdrDataOutputStream stream, SCMapEntry encodedSCMapEntry)
    {
        SCVal.Encode(stream, encodedSCMapEntry.Key);
        SCVal.Encode(stream, encodedSCMapEntry.Val);
    }

    public static SCMapEntry Decode(XdrDataInputStream stream, int maxDepth)
    {
        if (maxDepth <= 0)
            throw new IOException("Maximum decoding depth reached");
        maxDepth -= 1;
        var decodedSCMapEntry = new SCMapEntry();
        decodedSCMapEntry.Key = SCVal.Decode(stream, maxDepth);
        decodedSCMapEntry.Val = SCVal.Decode(stream, maxDepth);
        return decodedSCMapEntry;
    }

    public static SCMapEntry Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }
}
