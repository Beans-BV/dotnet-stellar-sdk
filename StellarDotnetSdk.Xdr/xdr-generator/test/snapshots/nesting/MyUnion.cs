// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  union MyUnion switch (UnionKey type)
//  {
//      case ONE:
//          struct {
//              int someInt;
//          } one;
//  
//      case TWO:
//          struct {
//              int someInt;
//              Foo foo;
//          } two;
//  
//      case OFFER:
//          void;
//  };

//  ===========================================================================
public class MyUnion
{
    public UnionKey Discriminant { get; set; } = new();

    public MyUnionOne One { get; set; }
    public MyUnionTwo Two { get; set; }

    public static void Encode(XdrDataOutputStream stream, MyUnion encodedMyUnion)
    {
        stream.WriteInt((int)encodedMyUnion.Discriminant.InnerValue);
        switch (encodedMyUnion.Discriminant.InnerValue)
        {
            case UnionKey.UnionKeyEnum.ONE:
                MyUnionOne.Encode(stream, encodedMyUnion.One);
                break;
            case UnionKey.UnionKeyEnum.TWO:
                MyUnionTwo.Encode(stream, encodedMyUnion.Two);
                break;
            case UnionKey.UnionKeyEnum.OFFER:
                break;
        }
    }

    public static MyUnion Decode(XdrDataInputStream stream, int maxDepth)
    {
        if (maxDepth <= 0)
            throw new InvalidDataException("Maximum decoding depth reached");
        maxDepth -= 1;
        var decodedMyUnion = new MyUnion();
        var discriminant = UnionKey.Decode(stream, maxDepth);
        decodedMyUnion.Discriminant = discriminant;
        switch (decodedMyUnion.Discriminant.InnerValue)
        {
            case UnionKey.UnionKeyEnum.ONE:
                decodedMyUnion.One = MyUnionOne.Decode(stream, maxDepth);
                break;
            case UnionKey.UnionKeyEnum.TWO:
                decodedMyUnion.Two = MyUnionTwo.Decode(stream, maxDepth);
                break;
            case UnionKey.UnionKeyEnum.OFFER:
                break;
            default:
                throw new InvalidDataException("Unknown discriminant value: " + discriminant.InnerValue);
        }

        return decodedMyUnion;
    }

    public static MyUnion Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }

    public class MyUnionOne
    {
        public int SomeInt { get; set; }

        public static void Encode(XdrDataOutputStream stream, MyUnionOne encodedMyUnionOne)
        {
            stream.WriteInt(encodedMyUnionOne.SomeInt);
        }

        public static MyUnionOne Decode(XdrDataInputStream stream, int maxDepth)
        {
            if (maxDepth <= 0)
                throw new InvalidDataException("Maximum decoding depth reached");
            maxDepth -= 1;
            var decodedMyUnionOne = new MyUnionOne();
            decodedMyUnionOne.SomeInt = stream.ReadInt();
            return decodedMyUnionOne;
        }

        public static MyUnionOne Decode(XdrDataInputStream stream)
        {
            return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
        }
    }

    public class MyUnionTwo
    {
        public int SomeInt { get; set; }
        public Foo Foo { get; set; }

        public static void Encode(XdrDataOutputStream stream, MyUnionTwo encodedMyUnionTwo)
        {
            stream.WriteInt(encodedMyUnionTwo.SomeInt);
            Foo.Encode(stream, encodedMyUnionTwo.Foo);
        }

        public static MyUnionTwo Decode(XdrDataInputStream stream, int maxDepth)
        {
            if (maxDepth <= 0)
                throw new InvalidDataException("Maximum decoding depth reached");
            maxDepth -= 1;
            var decodedMyUnionTwo = new MyUnionTwo();
            decodedMyUnionTwo.SomeInt = stream.ReadInt();
            decodedMyUnionTwo.Foo = Foo.Decode(stream, maxDepth);
            return decodedMyUnionTwo;
        }

        public static MyUnionTwo Decode(XdrDataInputStream stream)
        {
            return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
        }
    }
}
