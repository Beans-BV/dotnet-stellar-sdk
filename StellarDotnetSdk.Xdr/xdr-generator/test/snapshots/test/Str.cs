// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  typedef string str<64>;

//  ===========================================================================
public class Str
{
    public Str()
    {
    }

    public Str(string value)
    {
        InnerValue = value;
    }

    public string InnerValue { get; set; }

    public static void Encode(XdrDataOutputStream stream, Str encodedStr)
    {
        stream.WriteString(encodedStr.InnerValue);
    }

    public static Str Decode(XdrDataInputStream stream, int maxDepth)
    {
        if (maxDepth <= 0)
            throw new IOException("Maximum decoding depth reached");
        maxDepth -= 1;
        var decodedStr = new Str();
        decodedStr.InnerValue = stream.ReadString();
        return decodedStr;
    }

    public static Str Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }
}
