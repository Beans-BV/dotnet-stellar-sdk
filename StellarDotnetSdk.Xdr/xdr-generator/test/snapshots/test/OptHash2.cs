// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  typedef Hash* optHash2;

//  ===========================================================================
public class OptHash2
{
    public OptHash2()
    {
    }

    public OptHash2(Hash value)
    {
        InnerValue = value;
    }

    public Hash InnerValue { get; set; }

    public static void Encode(XdrDataOutputStream stream, OptHash2 encodedOptHash2)
    {
        if (encodedOptHash2.InnerValue != null)
        {
            stream.WriteInt(1);
            Hash.Encode(stream, encodedOptHash2.InnerValue);
        }
        else
        {
            stream.WriteInt(0);
        }
    }

    public static OptHash2 Decode(XdrDataInputStream stream, int maxDepth)
    {
        if (maxDepth <= 0)
            throw new InvalidDataException("Maximum decoding depth reached while decoding OptHash2");
        maxDepth -= 1;
        var decodedOptHash2 = new OptHash2();
        var OptHash2Present = stream.ReadInt();
        if (OptHash2Present != 0)
        {
            decodedOptHash2.InnerValue = Hash.Decode(stream, maxDepth);
        }
        return decodedOptHash2;
    }

    public static OptHash2 Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }
}
