// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  typedef Hash *optHash1;

//  ===========================================================================
public class OptHash1
{
    public OptHash1()
    {
    }

    public OptHash1(Hash value)
    {
        InnerValue = value;
    }

    public Hash InnerValue { get; set; }

    public static void Encode(XdrDataOutputStream stream, OptHash1 encodedOptHash1)
    {
        if (encodedOptHash1.InnerValue != null)
        {
            stream.WriteInt(1);
            Hash.Encode(stream, encodedOptHash1.InnerValue);
        }
        else
        {
            stream.WriteInt(0);
        }
    }

    public static OptHash1 Decode(XdrDataInputStream stream, int maxDepth)
    {
        if (maxDepth <= 0)
            throw new IOException("Maximum decoding depth reached");
        maxDepth -= 1;
        var decodedOptHash1 = new OptHash1();
        var OptHash1Present = stream.ReadInt();
        if (OptHash1Present != 0)
        {
            decodedOptHash1.InnerValue = Hash.Decode(stream, maxDepth);
        }
        return decodedOptHash1;
    }

    public static OptHash1 Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }
}
