// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  enum Color {
//    RED,
//    BLUE = 5,
//    GREEN
//  };

//  ===========================================================================
public class Color
{
    public enum ColorEnum
    {
        RED = 0,
        BLUE = 5,
        GREEN = 6,
    }

    public ColorEnum InnerValue { get; set; }

    public static Color Create(ColorEnum v)
    {
        return new Color
        {
            InnerValue = v,
        };
    }

    public static Color Decode(XdrDataInputStream stream, int maxDepth)
    {
        // maxDepth is intentionally not checked - enums are leaf types
        var value = stream.ReadInt();
        switch (value)
        {
            case 0: return Create(ColorEnum.RED);
            case 5: return Create(ColorEnum.BLUE);
            case 6: return Create(ColorEnum.GREEN);
            default:
                throw new InvalidDataException("Unknown enum value: " + value);
        }
    }

    public static Color Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }

    public static void Encode(XdrDataOutputStream stream, Color value)
    {
        stream.WriteInt((int)value.InnerValue);
    }
}
