// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  typedef int Multi;

//  ===========================================================================
public class Multi
{
    public Multi()
    {
    }

    public Multi(int value)
    {
        InnerValue = value;
    }

    public int InnerValue { get; set; }

    public static void Encode(XdrDataOutputStream stream, Multi encodedMulti)
    {
        stream.WriteInt(encodedMulti.InnerValue);
    }

    public static Multi Decode(XdrDataInputStream stream, int maxDepth)
    {
        if (maxDepth <= 0)
            throw new InvalidDataException("Maximum decoding depth reached");
        maxDepth -= 1;
        var decodedMulti = new Multi();
        decodedMulti.InnerValue = stream.ReadInt();
        return decodedMulti;
    }

    public static Multi Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }
}
