// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  enum UnionKey {
//    ERROR,
//    MULTI
//  };

//  ===========================================================================
public class UnionKey
{
    public enum UnionKeyEnum
    {
        ERROR = 0,
        MULTI = 1,
    }

    public UnionKeyEnum InnerValue { get; set; }

    public static UnionKey Create(UnionKeyEnum v)
    {
        return new UnionKey
        {
            InnerValue = v,
        };
    }

    public static UnionKey Decode(XdrDataInputStream stream, int maxDepth)
    {
        // maxDepth is intentionally not checked - enums are leaf types
        var value = stream.ReadInt();
        switch (value)
        {
            case 0: return Create(UnionKeyEnum.ERROR);
            case 1: return Create(UnionKeyEnum.MULTI);
            default:
                throw new InvalidDataException("Unknown enum value: " + value);
        }
    }

    public static UnionKey Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }

    public static void Encode(XdrDataOutputStream stream, UnionKey value)
    {
        stream.WriteInt((int)value.InnerValue);
    }
}
