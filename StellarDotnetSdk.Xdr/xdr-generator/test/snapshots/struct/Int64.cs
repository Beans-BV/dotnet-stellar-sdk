// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  typedef hyper int64;

//  ===========================================================================
public class Int64
{
    public Int64()
    {
    }

    public Int64(long value)
    {
        InnerValue = value;
    }

    public long InnerValue { get; set; }

    public static void Encode(XdrDataOutputStream stream, Int64 encodedInt64)
    {
        stream.WriteLong(encodedInt64.InnerValue);
    }

    public static Int64 Decode(XdrDataInputStream stream, int maxDepth)
    {
        if (maxDepth <= 0)
            throw new InvalidDataException("Maximum decoding depth reached while decoding Int64");
        maxDepth -= 1;
        var decodedInt64 = new Int64();
        decodedInt64.InnerValue = stream.ReadLong();
        return decodedInt64;
    }

    public static Int64 Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }
}
