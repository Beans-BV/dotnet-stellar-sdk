// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

using System;
using System.IO;

namespace StellarDotnetSdk.Xdr;

// === xdr source ============================================================

//  struct FreezeBypassTxs {
//      Hash txHashes<>;
//  };

//  ===========================================================================
public class FreezeBypassTxs
{
    public Hash[] TxHashes { get; set; }

    public static void Encode(XdrDataOutputStream stream, FreezeBypassTxs encodedFreezeBypassTxs)
    {
        var txHashesSize = encodedFreezeBypassTxs.TxHashes.Length;
        stream.WriteInt(txHashesSize);
        for (var i = 0; i < txHashesSize; i++)
        {
            Hash.Encode(stream, encodedFreezeBypassTxs.TxHashes[i]);
        }
    }

    public static FreezeBypassTxs Decode(XdrDataInputStream stream, int maxDepth)
    {
        if (maxDepth <= 0)
            throw new InvalidDataException("Maximum decoding depth reached while decoding FreezeBypassTxs");
        maxDepth -= 1;
        var decodedFreezeBypassTxs = new FreezeBypassTxs();
        var txHashesSize = stream.ReadInt();
        if (txHashesSize < 0)
            throw new InvalidDataException("txHashes size " + txHashesSize + " is negative");
        var txHashesRemainingInputLen = stream.GetRemainingInputLen();
        if (txHashesRemainingInputLen >= 0 && txHashesRemainingInputLen < txHashesSize)
            throw new InvalidDataException("txHashes size " + txHashesSize + " exceeds remaining input length " + txHashesRemainingInputLen);
        decodedFreezeBypassTxs.TxHashes = new Hash[txHashesSize];
        for (var i = 0; i < txHashesSize; i++)
        {
            decodedFreezeBypassTxs.TxHashes[i] = Hash.Decode(stream, maxDepth);
        }
        return decodedFreezeBypassTxs;
    }

    public static FreezeBypassTxs Decode(XdrDataInputStream stream)
    {
        return Decode(stream, XdrDataInputStream.DefaultMaxDepth);
    }
}
